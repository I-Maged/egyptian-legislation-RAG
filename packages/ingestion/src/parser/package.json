{
    "name": "egyptian-law-parser-v2-3",
    "version": "2.3.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "parse": "npm run build && node dist/index.js"
    },
    "dependencies": {
        "pdfjs-dist": "^5.4.149"
    },
    "devDependencies": {
        "@types/node": "^22.10.2",
        "typescript": "^5.7.2"
    }
}