import {ParsedArticle,ValidationIssue} from "../types.js";
export function validateArticles(a:ParsedArticle[],recoveryTasks:number){const out:ValidationIssue[]=[];const by=new Map<number,ParsedArticle[]>();for(const x of a){if(x.articleNumberNormalized===null)out.push({severity:"error",code:"INVALID_ARTICLE_NUMBER",message:"Cannot normalize article number.",articleNumber:x.articleNumber});if(!x.text.trim())out.push({severity:"error",code:"EMPTY_ARTICLE",message:"Article text is empty.",articleNumber:x.articleNumber});if(x.text.length>5000)out.push({severity:"warning",code:"LONG_ARTICLE",message:`${x.text.length} characters; preserve article integrity.`,articleNumber:x.articleNumber});if(x.pages.length>1)out.push({severity:"info",code:"MULTI_PAGE_ARTICLE",message:`Article spans ${x.pages.length} PDF pages.`,articleNumber:x.articleNumber});if(x.recoveryRecordCount)out.push({severity:"info",code:"RECOVERED_ARTICLE",message:"Article contains recovered OCR.",articleNumber:x.articleNumber});if(x.articleNumberNormalized!==null){const l=by.get(x.articleNumberNormalized)??[];l.push(x);by.set(x.articleNumberNormalized,l);}}for(const[n,l]of by)if(l.length>1)out.push({severity:"warning",code:"DUPLICATE_ARTICLE_NUMBER",message:`Article ${n} occurs ${l.length} times. This may be legitimate in a compound law; do not merge without component/instrument context.`,articleNumber:String(n)});if(recoveryTasks)out.push({severity:"info",code:"RECOVERY_QUEUE_NONEMPTY",message:`${recoveryTasks} page(s) require targeted OCR/recovery.`});return out;}